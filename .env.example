# SMTP Configuration for Booking Emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Ocena Apartment" <booking@ocenaliving.com>
ADMIN_EMAIL=booking@ocenaliving.com # Email address to receive booking notifications

# Firebase Configuration (Already managed via firebase-applet-config.json)
# GEMINI_API_KEY (Already managed by environment)

kind: AppHostingConfiguration
schemaVersion: v1

# Chỉ định lệnh để chạy server của Ocena
scripts:
  buildCommand: npm run build
  runCommand: npx tsx server.ts

# Cấu hình tài nguyên để chạy ổn định
runConfig:
  cpu: 1
  memoryMiB: 512